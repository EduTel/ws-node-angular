link(rel="stylesheet" type="text/css" href="index.css")
center
    h2=titulo
.table-responsive.table-striped
    h3 #{titulo_2} (Crudo) 
    div.xml
        code=xml_p
    h4 #{titulo_2} (Organizados)
    table.table.table-bordered
        //-console.log(estados_p);
        each fila in estados_p
            tr
            each campo in fila
                td=campo